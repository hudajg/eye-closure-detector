//    1
A = distance.euclidean(eye[0], eye[3])
B = distance.euclidean(eye[2], eye[4])
C = distance.euclidean(eye[1], eye[5])

//    2

A = distance.euclidean(eye[1], eye[5])
B = distance.euclidean(eye[2], eye[4])
C = distance.euclidean(eye[0], eye[3])
